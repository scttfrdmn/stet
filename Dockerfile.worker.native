FROM node:20-slim

WORKDIR /app

# Copy package files and install native deps
COPY package.json package-lock.json* ./
RUN npm ci --omit=dev

# Install AWS SDK (needed by worker)
RUN npm install @aws-sdk/client-s3

# Copy compiled worker
COPY dist/worker.cjs ./worker.cjs

CMD ["node", "worker.cjs"]
